<%- include("partials/header") %>

<h1>View and Search Your Favorite Photos</h1>
<div class="container">
    <% info["photos"].forEach(function(image){ %>
        <div class="thumbnail">
                <img src="<%= image.src.large %>" />
                <p>By: <strong><%= image.photographer %></strong></p>
        </div>
    <% }) %>
</div>

<%- include("partials/footer") %>